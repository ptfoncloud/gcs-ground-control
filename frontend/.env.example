# Optional. Only needed if the backend isn't reachable at
# http://<the-host-this-page-was-loaded-from>:8080 (the default the
# console derives automatically — see src/config.js).
# VITE_API_BASE_URL=http://192.168.1.50:8080
